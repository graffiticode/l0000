{
  "name": "@graffiticode/l0000-monorepo",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "L0000 — the root Graffiticode language (base language + inheritance kernel)",
  "workspaces": [
    "packages/core",
    "packages/api",
    "packages/view"
  ],
  "engines": {
    "node": ">=22"
  },
  "packageManager": "npm@10.9.2",
  "scripts": {
    "build": "npm run -w packages/core build && npm run -w packages/core build-static && npm run -w packages/api build && npm run -w packages/view build && npm run -w packages/view build:embed && npm run assemble",
    "assemble": "shx rm -rf packages/api/static && shx mkdir -p packages/api/static && shx cp -r packages/core/dist/static/. packages/api/static/ && shx cp -r packages/view/dist-embed/. packages/api/static/",
    "dev": "npm run -w packages/api dev",
    "start": "npm run -w packages/api start",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "test": "npm run -w packages/core test"
  },
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "@types/node": "^22.7.0",
    "eslint": "^9.13.0",
    "globals": "^15.11.0",
    "prettier": "^3.3.3",
    "shx": "^0.3.4",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.10.0",
    "vitest": "^2.1.3"
  }
}
